/* project.js — per-journey scroll choreography.
   Placeholder module. GSAP + ScrollTrigger + Lenis wiring lands here
   when the first full journey body is built (Phase 3). */
(function () {
  "use strict";
  // intentionally empty until the journey template gets its scroll sequences
})();
